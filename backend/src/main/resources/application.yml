spring:
  datasource:
    url: jdbc:mysql://localhost:3306/facebeat_db?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root  # 본인의 DB 아이디
    password: password # 본인의 DB 비밀번호
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update # 개발 단계에서는 update, 운영 시 validate 권장
    properties:
      hibernate:
        format_sql: true
        show_sql: true

jwt:
  secret: v3ryS3cr3tK3yF0rF4c3B34tRhyThmG4m3Pl34s3Ch4ng3Th1s # 32자 이상 임의 문자열
  expiration: 86400000 # 24시간 (밀리초)